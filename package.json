{
  "name": "d4umtest",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "husky": "^0.14.3",
    "react": "^16.2.0",
    "react-arcgis": "^3.1.1",
    "react-dazzle": "^1.2.7",
    "react-dom": "^16.2.0",
    "react-scripts": "1.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom && jest && pull-request-status",
    "eject": "react-scripts eject",
    "prepush": "npm run test && pull-request-status",
    "commitmsg": "validate-commit-msg"
  },
  "devDependencies": {
    "@projektionisten/pull-request-status": "^1.0.5",
    "validate-commit-msg": "^2.14.0"
  }
}
